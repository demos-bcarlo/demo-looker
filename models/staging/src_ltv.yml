version: 2

sources:
    - name: poc_looker
      database: 'leega-c4m-poc-gadgets.poc_looker'
      schema: poc_looker
      tables:
        - name: raw_dbt
          description: Dataset Raw do LTV.